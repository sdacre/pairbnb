  <nav class="navbar navbar-default">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span> 
        </button>
        <a class="navbar-brand $dark-color title" href="#">Wherebnb</a>
      </div>
      <div class="collapse navbar-collapse" id="myNavbar">
        <ul class="nav navbar-nav navbar-right">
          <li>
          <button type="button" class="btn btn-default buffer10">
						<%= link_to "Back to all listings", listings_path %>
					</button>
          <button type="button" class="btn btn-default buffer10">
            <%= link_to "Home", root_path %>
          </button>
          </li>
        </ul>
      </div>
   </div>
  </nav>

  <div class="container">
  <div class="row reservations">


  <div class="col-sm-8 reservations">
    <% @single_listing_reservations.each do |reservation| %>
    Check-in <%= single_listing_reservation.start_date %><br>
    Check-out <%= single_listing_reservation.end_date %><br>
    <% end %>
  </div>
<!-- links to other pages -->
  <div class="col-sm-4">
  </div>
</div>
</div>