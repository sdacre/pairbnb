  <nav class="navbar navbar-default">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span> 
        </button>
        <a class="navbar-brand $dark-color title" href="#">Wherebnb</a>
      </div>
      <div class="collapse navbar-collapse" id="myNavbar">
        <ul class="nav navbar-nav navbar-right">
          <li>
          <button type="button" class="btn btn-default buffer10">
						<%= link_to "Back to all listings", listings_path %>
					</button>
          <button type="button" class="btn btn-default buffer10">
            <%= link_to "Home", root_path %>
          </button>
          </li>
        </ul>
      </div>
   </div>
  </nav>

<div class="row">
	<div class="col-sm-4">
	</div>
	<div class="col-sm-4 container-fluid text-center top-buffer well">

    <!-- Start of test carousel -->


<div id = "carousel-example-generic" class= "carousel slide" data-ride= "carousel">
    <!-- Wrapper for slides -->
  <div class="carousel-inner">
  <% @single_listing.listing_image.each do |listing_image| %>
    <div class="item">
    <%= image_tag listing_image.main.url if @single_listing.listing_image? %>
    </div>
  <!-- Controls -->
   <a class="left carousel-control" href="#carousel-example-generic" role="button" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left"></span>
   </a>
   <a class="right carousel-control" href="#carousel-example-generic" role="button" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right"></span>
  </a>
  <% end %>
</div>


  <!--end of test carousel -->

		<%= @single_listing.summary %><br>
		Address:<%= @single_listing.address %><br>
		<%= @single_listing.home_type %><br>
		<%= @single_listing.listing_type %><br>
		$<%= @single_listing.price%><br>
		Bedrooms:<%= @single_listing.bedroom %><br>
		Bathrooms:<%= @single_listing.bathroom %><br>
		Accomodates:<%= @single_listing.accommodate %><br>

		<button type="button" class="btn btn-default bottom5">
			<%= link_to "Edit", edit_listing_path(@single_listing) %>
		</button><br>
		<button type="button" class="btn btn-default bottom5">
			<%= link_to "Delete", listing_path(@single_listing),
														:data => {:confirm => "You sure?",
                            :method => "delete"} %>
    </button><br>
		</div>

	<div class="col-sm-4">
	</div>
</div>
